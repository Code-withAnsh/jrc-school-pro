[build]
  base = "."        # repo root ka relative path
  publish = "."     # repo root me index.html hai
  command = ""      # plain HTML site, koi build command nahi

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200